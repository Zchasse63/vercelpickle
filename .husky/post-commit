#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Run the refactoring status updater
node scripts/update-refactoring-status.js

# If the status file was updated, commit it
if git diff --name-only | grep -q "REFACTORING_STATUS.md"; then
  git add REFACTORING_STATUS.md
  git commit --amend --no-edit
fi
